// new Console(options) 可构建自己的console
// console.assert(value[, ...message]) 根据value是否为真而判断打印message
// console.clear() 可test时清空控制台
// console.count([label]) 计算label输出次数,默认default
// console.countReset([label]) 重置上面
// console.group([...label]) 后面的console缩进加2空格
// console.time([label]) 计算与console.timeLog([label][, ...data]),console.timeEnd([label])等经过的ms
// console.trace([message][, ...args]) 打印当前堆栈追踪信息

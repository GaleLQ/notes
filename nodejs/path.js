
// path.basename(path[, ext]) 路径的最后一部分文件名
// path.dirname(path) 路径的目录
// path.extname(path) 后缀
// path.isAbsolute(path) 是否是绝对路径
// path.join([...paths]) 根据平台分隔符生成路径
// path.relative(from, to) 二者相对路径
// path.resolve([...paths]) 绝对
// path.sep 平台指定分隔符
// path.format(pathObject)
// path.parse(path)
// async function 返回promise
// block {}界定的复合语句
// break 中止当前,跳到后面(配合循环或label)
// class 创建一个基于原型继承的具有给定名称的新类,类似函数但不提升
// const 声明常量不可变
// continue 终止本次,跳到下次
// debugger 调用任何可用的调试功能,类似断个点
// default switch或export中使用
// do...while
// empty ;表示的空语句
// export 从模块中导出(默认导出和命名导出)供import,导出为严格模式
// for
// for await...of await一下可迭代对象每个值(常用异步生成器)
// for...in 遍历对象和其原型链上除Symbol的可枚举属性(不保证键顺序),Object.getOwnPropertyNames自己全部,Object.keys自己可枚举
// for...of在可迭代对象上根据定义创建一个迭代循环(重复使用生成器无效)
// function 注意整体提升
// function*
// if...else
// import 浏览器端需要script标签type为module
// import.meta 暴露特定上下文的元数据属性的对象(现有url)
// label 一条语句前面加个可以引用的标识符(配合continue和break)
// let
// return 结尾自动插入分号
// switch case
// throw 抛出一个用户自定义的异常,结尾自动插入分号
// try...catch...finally
// var 声明提升,赋值不提升
// while